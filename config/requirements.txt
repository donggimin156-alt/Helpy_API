# requirements.txt
# ============================================================
# 프로젝트 의존성 목록
#
# 설치: pip install -r config/requirements.txt
#
# [1차 Selenium 대응]
#   1차: selenium, webdriver-manager 등 브라우저 드라이버 의존성 포함
#   2차: 브라우저 없이 requests 만으로 동작 → 의존성 대폭 간소화
# ============================================================

# ── HTTP 클라이언트 ──────────────────────────────────────────
requests==2.32.3            # HTTP 요청 (1차의 selenium 대체)

# ── 테스트 프레임워크 ────────────────────────────────────────
pytest==8.3.5               # 테스트 실행 엔진
allure-pytest==2.13.5       # Allure 리포트 연동 (Epic/Feature/Story/Step)

# ── 응답 스키마 검증 ─────────────────────────────────────────
pydantic==2.10.6            # 응답 본문 타입 검증용 (schemas/ 에서 사용)

# ── 환경 변수 관리 ───────────────────────────────────────────
python-dotenv==1.0.1        # .env 파일 로드 (config/settings.py 에서 사용)

# ── 코드 품질 ────────────────────────────────────────────────
ruff==0.15.20               # 포맷 + 린트 통합 도구 (black + flake8/isort 대체)
